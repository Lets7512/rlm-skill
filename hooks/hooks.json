{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v bun &>/dev/null; then bun \"${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse-rlm.mjs\"; else node \"${CLAUDE_PLUGIN_ROOT}/hooks/pretooluse-rlm.mjs\"; fi"
          }
        ]
      }
    ]
  }
}
